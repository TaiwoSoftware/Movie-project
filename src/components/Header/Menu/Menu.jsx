import menuImage from '../images/Menu.svg';
const Menu = () => {
    return (
        <div>
            Sign In 
            <img src={menuImage} alt='menu' />
        </div>
    );
}

export default Menu;
